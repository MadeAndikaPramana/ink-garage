// No review text has been supplied yet. Reviews are third-party content, so
// this stays empty until the owner (or the developer, from the studio's own
// Google listing) hands over real quotes. Add entries as { name, meta, text }
// and Testimonials.jsx will start rendering the scrolling review columns.
export const TESTIMONIALS = []
